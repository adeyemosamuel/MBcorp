import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { LcexportPage } from './lcexport';

@NgModule({
  declarations: [
    LcexportPage,
  ],
  imports: [
    IonicPageModule.forChild(LcexportPage),
  ],
})
export class LcexportPageModule {}
